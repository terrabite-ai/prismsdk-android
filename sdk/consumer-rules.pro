# Applied to every app that links Prism. The public API is a Kotlin object with
# @JvmStatic members; R8 keeps what the app calls, so nothing extra is needed.
# The engine ships its own consumer rules.
