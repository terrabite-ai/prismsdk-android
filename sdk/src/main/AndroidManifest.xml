<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Location permissions are declared by the engine's manifest and merged
         into the app. The app still has to request them at runtime. -->
</manifest>
